# Moved

This placeholder predates the current experiment numbering, in which `03` is linear regression. The
MNIST milestones live in their own numbered directories:

- [Experiment 08: softmax regression on MNIST](../08_softmax_mnist/README.md)
- An MLP and a small CNN on MNIST are planned as milestones 09 and 12.

Datasets and trained artifacts are downloaded or generated locally and are not committed; see
[`scripts/download_mnist.sh`](../../scripts/download_mnist.sh).
